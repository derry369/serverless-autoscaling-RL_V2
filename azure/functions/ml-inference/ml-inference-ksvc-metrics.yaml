apiVersion: v1
kind: Service
metadata:
  name: ml-inference-metrics
  namespace: default
  labels:
    app: ml-inference
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"
spec:
  selector:
    serving.knative.dev/service: ml-inference
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 8080